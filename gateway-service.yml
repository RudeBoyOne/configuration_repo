server:
  port: ${PORT:8080}
  
spring:
  cloud:
    gateway:
      routes:
        - id: CatalogRoute
          uri: lb://CATALOG-SERVICE
          predicates:
            - Path=/catalog/**
        - id: MovieRoute
          uri: lb://MOVIE-SERVICE
          predicates:
            - Path=/movie/**
